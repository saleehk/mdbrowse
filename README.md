# mdbrowse

Browse and preview markdown files from any directory.

```
npx mdbrowse .
```

Zero-install CLI that spins up a local web UI with a file tree sidebar, rendered markdown, live reload, and optional Cloudflare Tunnel for remote access.

## Status

🚧 In development — Slice 1 (Core browser)
